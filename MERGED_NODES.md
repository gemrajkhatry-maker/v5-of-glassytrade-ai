# Merged v6 work marker

This file is a scratch ledger used by `scripts/triage_parallel_work.py` to decide which nodes are
no longer blockers. It is not a source of truth; the graph is. Edit it only when a node has been
merged to `main`.

- `DEC-2` — native stop order type + panic-flatten policy