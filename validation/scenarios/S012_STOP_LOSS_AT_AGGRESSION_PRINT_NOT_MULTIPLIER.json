{
  "scenario_id": "S012",
  "name": "STOP_LOSS_AT_AGGRESSION_PRINT_NOT_MULTIPLIER",
  "description": "SL = swing_low + (buffer_ticks * tick_size). NOT a fixed price multiplier.",
  "fabio_rule": "Stop Loss: just beyond the aggressive print. Add 1-2 tick buffer BEFORE swing high/low.",
  "input": {
    "aggression_print_price": 110.0,
    "direction": "LONG",
    "swing_low": 109.2,
    "tick_size": 0.05,
    "buffer_ticks": 2
  },
  "expected": {
    "stop_loss": 109.3,
    "stop_loss_formula": "swing_low + (buffer_ticks * tick_size)",
    "stop_NOT_at_swing_low": true
  }
}