# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
venv/
.venv/
env/
*.venv/

# Environment / secrets (never commit live credentials)
.env
.env.local
# Root token-state dir only — an un-anchored `runtime/` also swallowed the
# trading/src/tradex_trading/runtime/ Python package (the boot path was never
# committed; fresh clones could not boot).
/runtime/
# Broker runtime state under trading/ (instrument caches, token/totp state).
trading/runtime/

# Frontend (greenfield AMT terminal)
node_modules/
frontend/dist/
*.tsbuildinfo

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/

# graphify knowledge-graph output (regenerated; see `graphify update .`)
graphify-out/
