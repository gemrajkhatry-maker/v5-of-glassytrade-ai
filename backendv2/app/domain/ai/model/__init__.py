"""AI domain model package.

This package is intentionally empty. Domain model types used by the RL layer are
defined in ``app.domain.ai.rl`` (AMTObservation, TradeResult, DataSplit).

Import from there directly:

    from app.domain.ai.rl import AMTObservation, DataSplit
"""

__all__: list[str] = []
