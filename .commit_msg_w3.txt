refactor(v7 wave 3): one constants module; delete quant/config and orphaned thresholds

N6 - quant/config/constants.py (a 1-file package) duplicated
quant/contracts/constants.py values: MIN_RR_RATIO was defined in both, and
DETERMINISTIC_CONVICTION (0.7) + CONFIDENCE_HIGH_THRESHOLD (0.65) formed the
constant-true provenance gate removed in N3. Every still-consumed engine
constant (WARMUP_BARS, CVD_KILL_THRESHOLD, SEED_CACHE_TTL_SECONDS,
TICK_SIZE_NSE_OPTIONS, MAX_STOP_DISTANCE_TICKS, DEFAULT_RING_SIZE,
JSONL_FLUSH_BATCH, DEFAULT_TIME_STOP_BARS, ABSORPTION_MAX_AGE_BARS,
OBI_AGGRESSION_THRESHOLD) moved to quant/contracts/constants.py; the
package and its __init__ star-export are deleted; the six production
importers (runtime, amt_engine, hotpath, gates_edge, gates_rr,
signal_builder) are repointed.

Also removed: the dead runtime imports of DETERMINISTIC_CONVICTION and
WARMUP_BARS (imported, never used - context_builder has its own literal),
the orphaned CONFIDENCE_HIGH_THRESHOLD and CONFIDENCE_LOW_THRESHOLD /
AGENT_DECISION_THRESHOLD / MIN_GRADE_SCORE_THRESHOLD / session-phase /
pyramid constants (zero production readers), and the misleading
base.yaml/comments claiming LVN_THRESHOLD/HVN_THRESHOLD were dead (the
analyzer config genuinely consumes them - they stay).

tests/quant/test_constants.py now pins the consolidated module, asserts the
old package stays deleted, and guards against the conviction-vs-threshold
comparison returning (N3/N6 regression guard).

Merge gate: 1098 passed, 2 skipped (architecture + decision + execution +
runtime + contracts). Ruff: no new findings from this change (1 pre-existing
E402 in contracts/constants.py unchanged).

🤖 Generated with Codebuff
Co-Authored-By: Codebuff <noreply@codebuff.com>
