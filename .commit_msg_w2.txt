refactor(v7 wave 2): flatten decision gates package; verify Dhan adapter split

N4 - the quant/decision/gates/ package carried two real modules and two
pure pass-through wrappers around implementations that lived one level up
(gate_edge was literally `gate_edge = gate_triple_a_edge`; gate_risk_reward
delegated 1:1 to gates_rr). The two real gates moved to
quant/decision/gate_session_phase.py and gate_position_cooldown.py, the
wrappers and package are deleted, and pipeline.py imports the four real
modules directly. Pure move, zero behaviour change; test imports repointed.

N5 - verification only: the two Dhan adapters are distinct hexagonal ports
over one SDK (IMarketData vs IBroker, disjoint method sets), not
duplication. The original audit hypothesis is refuted in
docs/architecture/2026-09-21-dhan-adapter-split-verification.md; shared
symbol/exchange mapping already lives in _dhan_common.py. No production
changes needed. backend/tests/unit baseline unchanged (9 pre-existing
failures identical before and after; 1 module cannot collect due to a
missing jwt package in the backend venv - unrelated to this branch).

Merge gate: 961 passed (tests/architecture + decision + execution +
runtime).

🤖 Generated with Codebuff
Co-Authored-By: Codebuff <noreply@codebuff.com>
