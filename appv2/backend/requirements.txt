# AMT Live Trading System v2 — Backend Dependencies
# Python 3.11+

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0
websockets>=14.0
numpy>=1.26.0
scipy>=1.12.0  # For Black-Scholes normal distribution

# Reuses existing brokers/ library (not a pip package)
# brokers/ is imported from project root
aiohttp>=3.9.0
