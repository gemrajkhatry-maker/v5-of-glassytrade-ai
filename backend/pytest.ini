[pytest]
pythonpath = . .. src
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (may require infrastructure)
    slow: Slow tests (>5s, excluded from quick runs)
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
