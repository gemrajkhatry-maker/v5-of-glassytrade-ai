
import { DEFAULT_CHART_CONFIG, ChartConfig } from './config';

// Re-export for backward compatibility
export { type ChartConfig, DEFAULT_CHART_CONFIG } from './config';
export { IST_OFFSET_SECONDS } from './time/ist';

// Alias for backward compatibility
export const DEFAULT_CONFIG = DEFAULT_CHART_CONFIG;
