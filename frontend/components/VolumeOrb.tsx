
import React from 'react';
import { RoundedBox } from '@react-three/drei';

// This component is deprecated in favor of VolumeAccessory and is kept stubbed to prevent build errors.
const VolumeOrb: React.FC<any> = () => {
  return null;
};

export default VolumeOrb;
